# Kali templates (placeholder)

This directory is reserved for future Kali Packer configurations.
Planned layout mirrors `packer/windows`:

- `build.kali.pkr.hcl`
- `variables.pkr.hcl`
- `vars/` (provider or flavor var files)
- `scripts/` (provisioning helpers)
