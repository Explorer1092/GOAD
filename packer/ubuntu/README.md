# Ubuntu templates (placeholder)

This directory is reserved for future Ubuntu Packer configurations.
Planned layout mirrors `packer/windows`:

- `build.ubuntu.pkr.hcl`
- `variables.pkr.hcl`
- `vars/` (provider or flavor var files)
- `scripts/` (provisioning helpers)
